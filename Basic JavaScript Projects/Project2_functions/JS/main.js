function myFunction() {
    var sentence = "The wizard says to watch out for Goblins";
    sentence += " Their numbers are many and they don't like wizards"; // Concatenate the sentences
    document.getElementById("Concatenate").innerHTML = sentence; // Display the concatenated text in the HTML element
}

function warningFunction() {
    var warning = "You went the wrong way!"; // Define the warning message
    document.getElementById("Warning").innerHTML = warning; // Display the warning message in the HTML element
}